Trying to setup the config with LUA.

```
With this new config (@home/.config/hypr), would it be possible to when have my environment be a bit different? Don't make changes yet but my goal would be to press super + number and all 3 monitors would jump. Right now each monitor is locked to a specific item. I'd rather it be all 3 monitors linked to something though
```
